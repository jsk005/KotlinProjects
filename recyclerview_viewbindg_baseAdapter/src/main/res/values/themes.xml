<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item> <!-- 버튼 배경색상, Toolbar 상단바 색상 -->
        <item name="android:textColorPrimary">@color/primaryTextColor</item> <!-- 툴바 홈버튼(뒤로가기) 색상 -->
        <item name="colorPrimaryVariant">@color/blue_200</item> <!-- 스먀트폰 최상단 색상 -->
        <item name="colorOnPrimary">@color/black</item> <!-- 글자색상 -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#ff625b71</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="backgroundColor">@color/white</item> <!-- CordinatorLayout 배경색상 -->
        <item name="android:windowBackground">@color/white</item> <!-- window 배경색상, 별도 지정하지 않으면 white -->
    </style>

    <style name="AppTheme.AppBarOverlay"  parent="ThemeOverlay.MaterialComponents.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="AppTheme.ErrorAppearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorRed</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Toolbar" parent="Widget.MaterialComponents.Toolbar">
        <item name="titleTextAppearance">@style/TextAppearance.MaterialComponents.Body2</item>
    </style>


</resources>